import type { Config } from 'tailwindcss'

const config: Config = {
  darkMode: ['class'],
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        background: '#08080a',
        surface:    '#0f0f12',
        surface2:   '#16161a',
        surface3:   '#1e1e24',
        border: {
          DEFAULT: '#242429',
          subtle:  '#1a1a1f',
          strong:  '#36363e',
        },
        blue: {
          DEFAULT: '#3b82f6',
          soft:    '#60a5fa',
          muted:   '#2563eb',
          dim:     '#1d4ed8',
          glow:    'rgba(59,130,246,0.15)',
        },
        accent: '#60a5fa',
        text: {
          primary:   '#f4f4f5',
          secondary: '#a1a1aa',
          muted:     '#52525b',
          dim:       '#3f3f46',
        },
        green:  { DEFAULT: '#22c55e', soft: '#4ade80', dim: '#15803d', glow: 'rgba(34,197,94,0.15)' },
        orange: { DEFAULT: '#f97316', dim: '#c2410c' },
        red:    { DEFAULT: '#ef4444', dim: '#b91c1c' },
        purple: { DEFAULT: '#a855f7', soft: '#c084fc', dim: '#7e22ce' },
        amber:  { DEFAULT: '#f59e0b', dim: '#b45309' },
        cyan:   { DEFAULT: '#06b6d4', dim: '#0e7490' },
      },
      fontFamily: {
        sans: ['var(--font-jakarta)', 'system-ui', 'sans-serif'],
        mono: ['var(--font-mono)', 'Consolas', 'monospace'],
      },
      fontSize: {
        '2xs': ['10px', '14px'],
        xs:    ['11px', '16px'],
        sm:    ['13px', '20px'],
        base:  ['14px', '22px'],
        md:    ['15px', '24px'],
        lg:    ['17px', '26px'],
        xl:    ['19px', '28px'],
        '2xl': ['22px', '30px'],
        '3xl': ['26px', '34px'],
      },
      letterSpacing: {
        tighter: '-0.04em',
        tight:   '-0.02em',
        normal:  '0em',
        wide:    '0.02em',
        wider:   '0.05em',
        widest:  '0.1em',
      },
      animation: {
        float:        'float 6s ease-in-out infinite',
        'pulse-glow': 'pulseGlow 3s ease-in-out infinite',
        'pulse-dot':  'pulseDot 2s ease-in-out infinite',
        'typing-dot': 'typingDot 1.4s ease-in-out infinite',
        'scroll-x':   'scrollX 40s linear infinite',
        'fade-in':    'fadeIn 0.3s ease forwards',
      },
      keyframes: {
        float:     { '0%, 100%': { transform: 'translateY(0px)' }, '50%': { transform: 'translateY(-12px)' } },
        pulseGlow: { '0%, 100%': { boxShadow: '0 0 20px rgba(59,130,246,0.2)' }, '50%': { boxShadow: '0 0 50px rgba(59,130,246,0.5)' } },
        pulseDot:  { '0%, 100%': { opacity: '1', transform: 'scale(1)' }, '50%': { opacity: '0.4', transform: 'scale(0.8)' } },
        typingDot: { '0%, 60%, 100%': { transform: 'translateY(0)' }, '30%': { transform: 'translateY(-4px)' } },
        scrollX:   { from: { transform: 'translateX(0)' }, to: { transform: 'translateX(-50%)' } },
        fadeIn:    { from: { opacity: '0', transform: 'translateY(4px)' }, to: { opacity: '1', transform: 'translateY(0)' } },
      },
      borderRadius: {
        sm:   '6px',
        DEFAULT: '8px',
        md:   '10px',
        lg:   '12px',
        xl:   '16px',
        '2xl':'20px',
        '3xl':'24px',
        full: '9999px',
      },
      boxShadow: {
        'card':   '0 1px 3px rgba(0,0,0,0.4), 0 4px 16px rgba(0,0,0,0.3)',
        'card-hover': '0 4px 24px rgba(0,0,0,0.5), 0 1px 0 rgba(255,255,255,0.04)',
        'glow-blue':  '0 0 24px rgba(59,130,246,0.25)',
        'glow-green': '0 0 20px rgba(34,197,94,0.2)',
        'inner-top':  'inset 0 1px 0 rgba(255,255,255,0.05)',
        'inset-blue': 'inset 0 0 20px rgba(59,130,246,0.06)',
      },
    },
  },
  plugins: [],
}

export default config
